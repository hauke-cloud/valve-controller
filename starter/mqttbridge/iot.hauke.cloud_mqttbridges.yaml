---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: mqttbridges.iot.hauke.cloud
spec:
  group: iot.hauke.cloud
  names:
    kind: MQTTBridge
    listKind: MQTTBridgeList
    plural: mqttbridges
    shortNames:
    - mb
    singular: mqttbridge
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.bridgeName
      name: Bridge
      type: string
    - jsonPath: .spec.host
      name: Host
      type: string
    - jsonPath: .status.connectionState
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              bridgeName:
                minLength: 1
                type: string
              credentialsSecretRef:
                properties:
                  name:
                    minLength: 1
                    type: string
                  namespace:
                    minLength: 1
                    type: string
                  passwordKey:
                    description: |-
                      Key in the Secret whose value is used as the MQTT password.
                      Defaults to "password" when empty.
                    type: string
                  usernameKey:
                    description: |-
                      Key in the Secret whose value is used as the MQTT username.
                      Defaults to "username" when empty.
                    type: string
                required:
                - name
                - namespace
                type: object
              deviceType:
                allOf:
                - enum:
                  - tasmota
                  - generic
                - enum:
                  - tasmota
                  - generic
                default: tasmota
                type: string
              discoveryEnabled:
                default: false
                type: boolean
              host:
                minLength: 1
                type: string
              maxReconnectBackoffSeconds:
                default: 60
                description: Reconnect backoff cap in seconds.
                format: int32
                maximum: 300
                minimum: 5
                type: integer
              port:
                default: 1883
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              topics:
                items:
                  properties:
                    qos:
                      default: 1
                      format: int32
                      maximum: 2
                      minimum: 0
                      type: integer
                    topic:
                      minLength: 1
                      type: string
                    type:
                      allOf:
                      - enum:
                        - telemetry
                        - result
                        - state
                        - command
                      - enum:
                        - telemetry
                        - result
                        - state
                        - command
                      type: string
                  required:
                  - topic
                  - type
                  type: object
                type: array
            required:
            - bridgeName
            - host
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectionState:
                allOf:
                - enum:
                  - connected
                  - disconnected
                  - connecting
                  - error
                - enum:
                  - connected
                  - disconnected
                  - connecting
                  - error
                type: string
              errorMessage:
                description: Human-readable error message when connectionState=error.
                type: string
              lastConnectedTime:
                format: date-time
                type: string
              lastDisconnectedTime:
                format: date-time
                type: string
              messagesReceived:
                description: Total MQTT messages received since last connect.
                format: int64
                type: integer
              reconnectCount:
                description: Number of reconnect attempts since startup.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
